import React from "react";
import { useSelector, useDispatch } from "react-redux";
import "./Account.css";
const Account = () => {
  const dispatch = useDispatch();
  //const pages = useSelector((state: any) => state.pages);

  const handleToggle = () => {};

  return (
    <div>
      <h1>Account Us! asas</h1>
    </div>
  );
};

export default Account;
